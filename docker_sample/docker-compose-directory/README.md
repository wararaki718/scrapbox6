# directory check

## build

```shell
docker-compose build
```

## run

```shell
docker-compose -f compose.yml up
```

```shell
docker-compose -f compose.yml -f commands/compose.hello.yml up
```

```shell
docker-compose -f compose.yml -f commands/compose.world.yml up
```
